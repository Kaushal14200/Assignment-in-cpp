#include<bits/stdc++.h>
using namespace std;
int main() {
    int age;
    cin>>age;
    if (age >= 18)
    {
        cout<<"eligible for own vote";
    }
    else
    {
        cout<<"not eligible for own vote";
    }
    return 0;
}
